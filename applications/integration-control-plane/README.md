# Integration Control Plane

Pure application contracts for organization-scoped provider connections,
destinations, credential bindings, and audit history.

This module has no Kernel, HTTP, database, OAuth SDK, or provider dependency. It
does not perform network calls and does not model users, roles, billing, legal
entities, or business authority.
