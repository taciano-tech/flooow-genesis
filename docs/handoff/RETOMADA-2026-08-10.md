# Flooow Genesis — checkpoint de retomada

**Registrado em:** 2026-08-05
**Retomada agendada:** 2026-08-10 10:26, America/Sao_Paulo
**Motivo da pausa:** renovação do plano informada pelo responsável pelo projeto.

## Estado confirmado no momento da pausa

- A implementação chegou à TASK-0051.
- O commit local atual é `79583ce` (`test: migrate controlled directional consumer`).
- A branch local é `agent/task-0051-controlled-consumer-migration`.
- A evidência da TASK-0051 registra PASS, build limpo, 24/24 traces e replicação
  independente sem divergências.
- A PR #47 foi informada como merged durante a execução anterior; confirmar o
  estado remoto ao retomar, pois a branch local ainda não foi sincronizada com
  `main` neste checkpoint.
- Nenhuma mudança produtiva posterior à TASK-0051 foi iniciada.

## Próximo trabalho autorizado

Executar a **TASK-0052 — Compatibility Review**, conforme
`docs/specifications/SPEC-0001-evidence-semantics-implementation.md`.

A TASK-0052 é exclusivamente revisão e documentação:

1. inventariar consumidores legados remanescentes;
2. inventariar construtores e APIs públicas legadas;
3. inventariar snapshots e documentação pública afetados;
4. decidir, para cada item, entre projeção, depreciação ou operação paralela;
5. não remover código legado nem alterar comportamento em runtime;
6. registrar evidência e executar o build de validação;
7. abrir PR independente para revisão.

Qualquer mudança produtiva encontrada nessa revisão exige uma nova especificação
aceita.

## Direção do MVP após a TASK-0052

Próxima proposta a escrever, sem implementar antes de sua aceitação:

- **TASK-0053 — MVP External API Contract**;
- API fina em Ktor;
- DTOs externos separados dos tipos do Kernel;
- primeiro fluxo síncrono de avaliação;
- nenhuma dependência de Ktor, persistência ou telemetria no Kernel.

Depois: PostgreSQL + Exposed JDBC + Flyway, Testcontainers, OpenTelemetry e
CloudEvents. Temporal deve permanecer como experimento posterior, fora do caminho
crítico do MVP.

## Procedimento obrigatório de retomada

1. Ler este arquivo e `AGENTS.md`.
2. Executar `git status`, confirmar branch, remotes, PR #47 e `main` remoto.
3. Não sobrescrever mudanças locais do usuário.
4. Sincronizar a base com segurança e criar uma branch exclusiva para TASK-0052.
5. Relê-la na especificação antes de produzir qualquer alteração.
6. Rodar `./gradlew clean build --rerun-tasks` antes de publicar.
