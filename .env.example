# Local-development values only. Replace the password outside isolated development.
POSTGRES_PASSWORD=flooow-local-only
FLOOOW_SERVICE_TOKEN=flooow-local-only-service-token-change-before-sharing
FLOOOW_ENVIRONMENT=local
FLOOOW_PORT=8080
